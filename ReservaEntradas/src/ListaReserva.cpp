#include "ListaReserva.h"

ListaReserva::ListaReserva() {
    cabeza = nullptr; // La lista empieza vacía
}

ListaReserva::~ListaReserva() {
    // Aquí podrías vaciar la lista, liberar nodos, etc.
    // Por ahora puede quedar vacío
}
