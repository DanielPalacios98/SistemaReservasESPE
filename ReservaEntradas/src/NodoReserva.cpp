#include "NodoReserva.h"

NodoReserva::NodoReserva() {
    reserva = nullptr;      // inicializa el puntero a null
    siguiente = nullptr;    // inicializa el puntero a null
}

NodoReserva::~NodoReserva() {
    // Si necesitas liberar memoria, lo haces aquí
    // (por ahora puede quedar vacío)
}
